query GetAllPosts($limit: Int!, $cursor: DateTime) {
	getAllPosts(limit: $limit, cursor: $cursor) {
		hasNext
		posts {
			id
			photo
			description
			createdAt
			user {
				id
				name
			}
			numLikes
			liked
		}
	}
}
