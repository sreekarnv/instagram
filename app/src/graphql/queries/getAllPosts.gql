query GetAllPosts($limit: Int, $cursor: DateTime) {
	query: getAllPosts(limit: $limit, cursor: $cursor) {
		count
		hasMore
		posts {
			id
			description
			updatedAt
			createdAt
			photo
			user {
				id
				email
				photo
				name
			}
		}
	}
}
