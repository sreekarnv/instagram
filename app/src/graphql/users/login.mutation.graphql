mutation Login($email: String!, $password: String!) {
	login(details: { email: $email, password: $password }) {
		user {
			id
			email
			name
		}
		errors {
			field
			message
		}
	}
}
